# Raw source videos are large and are not needed by the web app.
*.mp4

# Training images stay local; only the exported browser model is deployed.
dataset/
model-new/
model-new0.1/
screen_shot/

# Local Codex workspace metadata.
.agents/
.codex/
.playwright-cli/
